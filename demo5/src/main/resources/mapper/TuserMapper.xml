<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC ".//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo5.mapper.TuserMapper">
    <select id="login" resultType="com.example.demo5.entity.Tuser">
        SELECT * FROM tuser WHERE username=#{username}
    </select>
    <!--    <select id="getUserList" resultType="com.example.demo5.entity.Tuser">-->
    <!--        SELECT * FROM tuser-->
    <!--    </select>-->
</mapper>
